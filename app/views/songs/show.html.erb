
<h1> song Show Page <h1>

<h1><%=@song.name%><h1> <br>
<%= link_to "genre: #{@song.genre.name}", genre_path(@song.genre) %> <br>
<%= link_to "artist: #{@song.artist.name}", artist_path(@song.artist) %> <br>

<%= link_to "Back to songs index", songs_path %>